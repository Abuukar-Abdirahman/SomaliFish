# --- secrets: never commit these -------------------------------------------
.env
.env.*
!.env.example
.copernicusmarine/
*.credentials

# --- environment ------------------------------------------------------------
.venv/
venv/
__pycache__/
*.pyc
.ipynb_checkpoints/

# --- data (large, re-downloadable; see docs/DATA_SOURCES.md) ----------------
data/raw/
data/processed/
data/predictions/
*.nc
*.parquet
*.log

# data/demo/ IS committed on purpose: it is the offline fallback so the
# hackathon demo works with no network and no credentials.
!data/demo/*.json

# --- models -----------------------------------------------------------------
models/*.json
models/*.ubj

# --- os / editor ------------------------------------------------------------
.DS_Store
Thumbs.db
.vscode/
.idea/
